<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity
            android:name="ir.mostafa.launcher.nextcloud.LoginActivity"
            android:label="@string/preference_nextcloud"
            android:taskAffinity="ir.mostafa.launcher.nextcloud"
            android:theme="@style/NextcloudLoginTheme" />
    </application>

</manifest>