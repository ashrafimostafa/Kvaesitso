<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity
            android:name=".LoginActivity"
            android:label="@string/preference_nextcloud"
            android:taskAffinity="ir.mostafa.launcher.nextcloud"
            android:parentActivityName="ir.mostafa.launcher.ui.settings.SettingsActivity"
            android:theme="@style/OwncloudLoginTheme" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ir.mostafa.launcher.ui.settings.SettingsActivity" />
        </activity>
    </application>

</manifest>